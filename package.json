{
  "name": "fotosintesis-ai",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "pnpm --filter frontend dev",
    "build": "pnpm --filter frontend build",
    "lint": "pnpm --filter frontend lint",
    "eval": "sh -c 'if [ -x backend/.venv/bin/python ]; then EMBEDDING_DIMENSION=${EMBEDDING_DIMENSION:-8} backend/.venv/bin/python backend/scripts/run_evaluation.py --mode recorded --providers mock; else cd backend && EMBEDDING_DIMENSION=${EMBEDDING_DIMENSION:-8} python scripts/run_evaluation.py --mode recorded; fi'",
    "fallow": "pnpx fallow",
    "e2e:enrichment": "sh scripts/run-enrichment-e2e.sh"
  },
  "pnpm": {
    "overrides": {
      "next>sharp": "0.35.0",
      "next>postcss": "8.5.23",
      "postcss>nanoid": "3.3.18",
      "sass>immutable": "5.1.8"
    }
  }
}