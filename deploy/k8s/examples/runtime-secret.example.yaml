# Non-applied example only. Do not apply this file with placeholder values and do not
# commit real secret values. Create the runtime Secret from approved secret sources.
apiVersion: v1
kind: Secret
metadata:
  name: fotosintesis-runtime
  namespace: fotosintesis
type: Opaque
stringData:
  database-url: "postgresql+asyncpg://USER:PASSWORD@HOST:5432/fotosintesis"
  auth-secret: "replace-outside-source-control"
