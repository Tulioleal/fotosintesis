## 1. Fail-Closed Regression Tests

- [x] 1.1 Add focused `frontend/auth.ts` tests that accept a complete valid credentials response and deny invalid JSON, missing/empty user identity, missing/empty `session_token`, invalid email, non-Boolean verification state, and invalid `session_expires_at` without creating partial authenticated state; directly test malformed initial JWT and browser-session callback state with sentinel values.
- [x] 1.2 Extend `frontend/src/lib/server/backend-session.test.ts` for a missing secret even when a backend cookie exists, invalid `AUTH_URL`, decoder rejection, missing/non-string/empty `backendCredential`, and backend cookie preservation when a valid secret exists; assert every invalid JWT/configuration case returns `null` without exposing the sentinel secret, cookie, token, callback payload, or exception text through application-owned diagnostics.
- [x] 1.3 Extend `frontend/src/middleware.test.ts` to prove decoder failure, backend `401`, and backend validation transport failure redirect private routes to `/login` with the existing safe callback URL while public routes remain unaffected.
- [x] 1.4 Extend the logout and browser-session boundary tests to preserve server-side backend invalidation and prove `backendCredential` remains absent from browser-readable session state and failure responses; verify application-owned callback errors are generic and contain no sentinel callback values.

## 2. Auth.js Dependency Alignment

- [x] 2.1 Set `frontend/package.json` to exact `next-auth: "5.0.0-beta.32"`, remove direct `@auth/core`, and make no unrelated dependency upgrades.
- [x] 2.2 Regenerate `pnpm-lock.yaml` with the repository-pinned pnpm version and verify it resolves exactly beta 32 with one transitive `@auth/core` version and no direct core importer.
- [x] 2.3 Replace every production and test import/mock of `@auth/core/jwt` with `next-auth/jwt`; verify no application or test source imports any `@auth/core` path.

## 3. Authentication Boundary Implementation

- [x] 3.1 Add runtime Zod validation for the successful credentials response consumed by `frontend/auth.ts`, requiring the complete concrete user identity, non-empty server-only session token, and valid session expiration before `authorize` returns a user.
- [x] 3.2 Make credentials JSON parsing/schema failures deny authentication without returning partial identity or credential state; extract testable JWT/session callback functions that require concrete validated initial state and fail malformed callback state with one generic value-free authentication error while preserving valid login, JWT, session, and callback behavior.
- [x] 3.3 Make `resolveBackendAuthHeaders` require a configured secret before accepting either the backend-cookie or JWT path, then return `null` for invalid Auth.js configuration, decoder exceptions, malformed token state, or absent concrete backend credentials without exposing sensitive values or raw exception text.
- [x] 3.4 Make `validateBackendSession` return `false` when credential resolution fails, the backend rejects the session, or backend session validation cannot complete; preserve backend `GET /auth/session` as authoritative.
- [x] 3.5 Audit middleware, route handlers, server helpers, and callbacks for `req.auth`, `request.auth`, bare `auth()` result truthiness, and existence-only authentication checks; replace any finding with concrete credential/identity checks and authoritative backend validation, and record the zero-finding or remediated file list in the implementation handoff.

## 4. Dependency Security Gate

- [x] 4.1 Add `frontend/scripts/check-auth-dependencies.mjs` and a frontend package command that fail unless the manifest and installed production graph contain exact beta 32, no direct core declaration, exactly one reachable transitive core version, and no `@auth/core` imports under frontend application/test source.
- [x] 4.2 Add focused tests for the dependency policy script using controlled fixture manifests/graphs/source imports so vulnerable beta 31, an open version range, direct core, duplicate core versions, and direct core imports each fail while the intended graph passes.
- [x] 4.3 Update `.github/workflows/frontend-ci.yml` path filters to include root `package.json`, `pnpm-lock.yaml`, and `pnpm-workspace.yaml` changes.
- [x] 4.4 Run the deterministic authentication dependency check and `pnpm audit --prod --audit-level high` after frozen installation and before build/image publication; do not add an ignore for GHSA-8fpg-xm3f-6cx3.
- [x] 4.5 Add a bounded Node smoke command that starts the built production frontend on an isolated port with `AUTH_SECRET` and `NEXTAUTH_SECRET` removed, asserts a representative private route redirects to `/login` without private content, always terminates the server, and runs in frontend CI after build.

## 5. Focused and Full Verification

- [x] 5.1 Run the dependency policy script tests and command, then confirm `pnpm list next-auth @auth/core --prod --depth Infinity` shows exact beta 32 and one core line and `pnpm audit --prod --audit-level high` reports no blocking production advisory.
- [x] 5.2 Run focused Auth.js configuration, credentials, backend-session, middleware, logout, callback URL, and browser credential non-exposure tests.
- [x] 5.3 Run `corepack pnpm --filter frontend lint`, `corepack pnpm --filter frontend typecheck`, `corepack pnpm --filter frontend test`, `corepack pnpm --filter frontend openapi:check`, `corepack pnpm --filter frontend build`, and the invalid-configuration production smoke command.
- [x] 5.4 Run the authentication Playwright journey in `frontend/e2e/auth-home.spec.ts` against the documented local E2E stack and record passed/skipped counts.
- [x] 5.5 Run `openspec validate "upgrade-authjs-security-boundary" --strict` and `git diff --check`, then review the diff for unrelated dependency upgrades, browser credential exposure, weakened backend authority, or affected-version rollback instructions.

## 6. Independent Verification Handoff

- [x] 6.1 Leave `upgrade-authjs-security-boundary` active and unarchived after implementation; report exact commands/results, dependency versions, guard-audit findings, changed files, and any skipped verification to the independent verifying agent.
- [x] 6.2 Do not synchronize delta specs or archive the change; independent verification must compare implementation against proposal, design, every scenario, and this checklist before archive.

## 7. Verification Remediation

Independent verification (`verify/upgrade-authjs-security-boundary.verify.md`) found three critical issues and one warning. The change is reopened for remediation; it must not be archived or synchronized until every item below passes.

- [x] 7.1 Harden the browser-session callback so malformed JWT state is rejected: `createJwtToken` sets `token.sub` from the validated user ID; a callback-token schema requires a non-empty `sub`, non-empty string `backendCredential`, parseable `sessionExpiresAt`, and Boolean `email_verified`; `createBrowserSession` requires both `session.user` and valid callback-token state; invalid state throws only `AUTHENTICATION_ERROR`; the returned browser session exposes no `backendCredential` or expiration.
- [x] 7.2 Add callback-validation tests rejecting missing/empty `sub`, missing/empty/non-string `backendCredential`, missing/invalid `sessionExpiresAt`, non-Boolean `email_verified`, and missing `session.user`; assert errors contain no sentinel identity, credential, token, or expiration values; assert valid browser sessions expose only safe identity fields.
- [x] 7.3 Harden decoded JWT handling in `resolveBackendAuthHeaders`: validate the decoded token against the same concrete subject/credential/expiration/verification-state shape used by callbacks; return `null` for malformed state without logging raw values; preserve the backend-cookie path and authoritative `GET /auth/session` validation.
- [x] 7.4 Add decoded-token validation tests for every required field; prove malformed state never reaches backend session validation; prove valid state still forwards the server-only credential; prove backend rejection and transport failure still deny private access.
- [x] 7.5 Make the dependency policy deterministic and network-independent: remove its `pnpm audit` behavior; inspect `dependencies`, `devDependencies`, `optionalDependencies`, and `peerDependencies`; scan all frontend source and test directories including `scripts`; detect static, side-effect, re-export, dynamic, and CommonJS `require` imports; construct fixture strings dynamically so the policy test file itself is scanned.
- [x] 7.6 Add dependency-policy fixture tests for every manifest section and import form so each bypass fails while the intended graph passes.
- [x] 7.7 Remediate production advisories: pin `next` exactly to `15.5.21`, regenerate the lockfile, add only the scoped root overrides still required by the audit (`next>sharp` >= 0.35.0, `next>postcss` >= 8.5.23, `postcss>nanoid` >= 3.3.18, `sass>immutable` >= 5.1.8), keep `next-auth@5.0.0-beta.32` and `@auth/core@0.41.3` unchanged, add no advisory ignores, and rerun `pnpm audit --prod --audit-level high` until it exits zero.
- [x] 7.8 Enforce the high-severity production audit in CI: add a blocking `pnpm audit --prod --audit-level high` step after frozen installation and before build or image publication.
- [x] 7.9 Add a deterministic authentication E2E journey against a loopback-only Node backend stub: private callback login, active browser session with safe identity fields, no browser-visible backend credential/expiration, logout, backend invalidation recording, frontend state clearing, and subsequent private-route denial.
- [x] 7.10 Run the full verification sequence (audit, policy, focused and full tests, lint, typecheck, OpenAPI check, production build, fail-closed smoke, and deterministic auth E2E) and record exact results.
- [x] 7.11 Write `verify/upgrade-authjs-security-boundary.implementation.md` with exact commands/results, dependency versions, audit result, test counts, Playwright counts, guard-audit findings, changed files, and any unavailable checks; leave the change active and unarchived for independent verification.

## 8. Durable Enforcement Remediation

The second verification confirmed the critical issues were addressed but that enforcement must be durable, not example-shaped. The change stays active and unarchived; the independent verification report is not modified and specs are not synchronized.

- [x] 8.1 Enforce the authentication E2E in CI before image publication: install the matching Playwright Chromium after dependency installation and run `pnpm test:e2e:auth` after the production build and fail-closed smoke; the image build job remains dependent on the validate job so no image or deployment can proceed unless the journey passes.
- [x] 8.2 Replace regex import scanning with AST-based detection using the existing `@typescript-eslint/parser` `parseForESLint` API: parse `.ts`, `.tsx`, `.js`, `.jsx`, `.mjs`, and `.cjs`; walk visitor keys inspecting `ImportDeclaration`, `ExportNamedDeclaration`, `ExportAllDeclaration`, `ImportExpression`, CommonJS `require()`, `TSImportEqualsDeclaration`, and `TSImportType`; reject specifiers equal to `@auth/core` or starting with `@auth/core/`; statically evaluate string literals, static template literals, parenthesized expressions, string concatenations, and TS assertion/non-null wrappers; fail the policy check when a scanned source file cannot be parsed; preserve `file:line` diagnostics.
- [x] 8.3 Add scanner fixtures for computed and comment-separated imports (`import("@auth/" + "core")`, `require("@auth/" + "core/jwt")`, `import/*gap*/("@auth/core")`, `require/*gap*/("@auth/core/jwt")`, `import(`@auth/${"core"}/jwt`)`), TypeScript import-equals and import types, exact line reporting, parse failures, safe strings and comments that only mention `@auth/core`, and `@auth/coreish`/`@auth/core-client` allowed controls; retain the existing literal-form and manifest-section fixtures.
- [x] 8.4 Complete callback negative tests with true absent fields: clone the token and `delete` the property for absent `sub`, `backendCredential`, `sessionExpiresAt`, and `email_verified` (not represented by `""`); cover empty and non-string variants; cover absent `session.user`; every case asserts the error is exactly `AUTHENTICATION_ERROR`, that no sentinel identity/credential/expiration/raw validation info is present, and that no browser session is returned.
- [x] 8.5 Use one E2E port source: `frontend/e2e/auth-home.spec.ts` derives `STUB_URL` from `AUTH_STUB_PORT` (default 8099) identically to `playwright.auth.config.ts`; verify the suite on non-default ports.
- [x] 8.6 Assert the actual backend credential: after login, capture the stub's active session token from `GET /__test__/state`, assert the serialized browser session does not contain that exact token nor `backendCredential`/`sessionExpiresAt`/expiration fields; after logout, require a logout event containing that exact token, require the token absent from active sessions, confirm no authenticated user in `/api/auth/session`, and confirm private-route denial.
- [x] 8.7 Run focused and full verification and update `verify/upgrade-authjs-security-boundary.implementation.md` with per-case callback results, scanner adversarial fixtures, non-default ports, the actual-token confidentiality and exact-token invalidation assertions, workflow ordering, full command results, and any unavailable checks.

## 9. Final Guard Hardening

Third-round hardening of the durable guards. The change stays active and unarchived; neither independent verification report is modified and specs are not synchronized.

- [x] 9.1 Validate Auth.js core graph ancestry in `verifyInstalledGraph`: traverse every project returned by `pnpm list`; find every reachable `next-auth` node and require at least one; require every reachable `next-auth` version to equal `5.0.0-beta.32`; collect `@auth/core` descendants beneath those `next-auth` nodes and require exactly one descendant version equal to `0.41.3`; continue rejecting additional `@auth/core` versions elsewhere in the production graph.
- [x] 9.2 Add core-ancestry fixtures: valid core beneath `next-auth`; core only beneath an unrelated package; core missing beneath `next-auth`; vulnerable core beneath `next-auth`; multiple core versions beneath separate `next-auth` nodes; multiple projects where a later project contains a vulnerable Auth.js graph.
- [x] 9.3 Resolve immutable string bindings used as module specifiers (`const target = "@auth/core"; import(target);` and `const jwtPackage = "@auth/" + "core/jwt"; require(jwtPackage);`) using `parseForESLint` scope information, not a global identifier map: resolve only a lexical `const` with exactly one definition whose initializer is statically evaluable and not reassigned, with no identifier cycle; continue supporting literals, templates, concatenations, parentheses, and TS wrappers.
- [x] 9.4 Add identifier-binding fixtures: static `const` dynamic import; static `const` CommonJS require; concatenated static binding; nested lexical scopes with the same identifier; reassigned `let` as unresolved; function-produced specifier as unresolved; cyclic bindings as unresolved; shadowed local `require` as allowed rather than treated as CommonJS loading.
- [x] 9.5 Fix parser maintenance: for `TSImportType` read `node.source ?? node.argument` so the deprecated argument getter is not accessed on current parser versions; derive a real parse-failure line from `error.lineNumber`, `error.loc?.line`, `error.index` converted to a line, then fallback 1 — never report line 0; update tests to expect the actual malformed-source line; the focused scanner suite must run with `NODE_OPTIONS=--throw-deprecation` with zero deprecation warnings.
- [x] 9.6 Complete the browser-callback matrix with non-string `sub`, non-string `sessionExpiresAt`, and empty `email_verified` cases; every malformed callback case asserts an exception, message equals `AUTHENTICATION_ERROR`, no sentinel value, no credential/token/expiration/validation detail, and no session returned.
- [x] 9.7 Complete the credentials-response matrix with genuinely absent backend-response fields (absent `user`, `user.id`, `user.name`, `user.email`, `user.email_verified`, `session_token`, `session_expires_at` via property deletion) plus the existing empty/invalid/non-Boolean/invalid-JSON cases; every malformed response returns `null` without partial authenticated state.
- [x] 9.8 Return a safe browser-session projection: `createBrowserSession` constructs a new session containing only `expires` and `user: { id, name, email, image, email_verified }` from validated state instead of mutating and returning the entire Auth.js-provided session; add a test seeding forbidden top-level and user-level `backendCredential`/`sessionExpiresAt` properties and asserting none survive serialization while normal fields remain correctly mapped.
- [x] 9.9 Run focused verification (deprecation-promoted scanner suite, policy command, and focused auth tests) with required outcomes: unrelated-core graph fails; static identifier imports fail; shadowed non-module `require` is not falsely rejected; parse failures report a real line; no parser deprecation; every missing/empty/invalid/non-string callback case passes; forbidden session input fields are removed by projection.
- [x] 9.10 Run full verification (frozen install, dependency list, audit, policy, full tests, lint, typecheck, OpenAPI check, build, fail-closed smoke, non-default-port auth E2E, `openspec validate --strict`, `git diff --check`, and `actionlint` reported if unavailable) and update `verify/upgrade-authjs-security-boundary.implementation.md` with core-ancestry fixtures, identifier-binding fixtures, parser warning count, parse-error line assertion, complete callback and credentials matrices, safe session projection assertion, focused/full counts, audit and E2E results, and any unavailable checks.
